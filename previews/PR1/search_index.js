var documenterSearchIndex = {"docs":
[{"category":"section","location":"concepts/weights/#Weights-and-gaps","page":"Weights and gaps","text":"","title":"Weights and gaps"},{"category":"section","location":"concepts/weights/#Weights","page":"Weights and gaps","text":"weights weights every statistic of a request:\n\nblockstats(x, [8]; stats = (Mean(), Var()), weights = w)                  # one weight per element\nblockstats(x, [8]; stats = (Mean(), Var()), weights = (nothing, dz))      # per-axis factors\nblockstats(x, [8]; stats = (Mean(),), weights = (z = dz,), dimnames = (:x, :z))\n\nPer-axis factors are multiplied per element, so the N-dimensional weight array is never materialized — a cell-thickness vector along one axis costs a vector, not a copy of the input.\n\nWeighting is a property of the request, not of a tag, so nothing can end up half-weighted. A statistic with no weighted form throws when the request is prepared:\n\nWeighted Not weighted\nMean, Sum, Var, Std, Cov, Corr, ProductMean Min, Max, Extrema, Moments, CentralMoments, Skewness, Kurtosis\n\nCount() passes through unchanged: how many observations a window holds does not depend on their weights. For the effective sample size, ask for the total weight itself with Component(Mean(), :W).\n\nThe arithmetic is West's weighted lift with the Chan/Pébay merge run on weight totals instead of counts. Unit weights reproduce an unweighted request bit for bit.","title":"Weights"},{"category":"section","location":"concepts/weights/#Corrections","page":"Weights and gaps","text":"Var, Std and Cov take corrected:\n\nValue Denominator\nfalse W (population)\ntrue, :frequency W - 1 — weights are counts of repeated observations\n:reliability W - W₂/W — weights are relative precisions\n\nUnit weights make the two corrected forms identical (W - W₂/W = n - 1), so the unweighted accumulators accept the symbols too. The frequency denominator is clamped at zero, so a total weight below one gives NaN rather than a negative variance.","title":"Corrections"},{"category":"section","location":"concepts/weights/#Gaps","page":"Weights and gaps","text":"skipnan = true drops non-finite observations:\n\nblockstats(x, [8]; stats = (Count(), Mean(), Var()), skipnan = true)\n\nThe skip is per statistic, not per element. A composite lifts each member from its own bound fields, so a gap in u removes that element from Mean(:u) and from Cov(:u, :w), but leaves Mean(:w) untouched. Count() then reports the number of finite observations, and a window with none reports NaN for its mean and variance.\n\nCounts vary per cell once observations can be dropped, so the workspace stops storing a window's count once for the whole node and keeps one per cell. That costs memory; it is the price of the feature and is only paid when you ask for it.\n\nWeights and skipnan compose: an element is dropped if any of the statistic's own bound fields — the weight included — is not finite.","title":"Gaps"},{"category":"section","location":"concepts/windows/#Windows","page":"Windows","text":"A window is where a statistic is computed. Everything in this package — a tile, a sliding window, a set of windows at hand-picked anchors — is the same object, which is what lets one planner span all of them.\n\n(Image: Tiles, strided windows, anchors and anisotropic windows)","title":"Windows"},{"category":"section","location":"concepts/windows/#One-axis-at-a-time","page":"Windows","text":"An AxisWindow is size cells at a set of positions along an axis of extent cells. Origins are 0-based offsets, so the window at origin o covers 1-based indices o+1 : o+size.\n\nPositions come in two forms:\n\nProgression(offset, stride, count) — an arithmetic progression. Tiles are the stride == size case; sliding windows are stride < size; stride == 1 is dense.\nOrigins(v) — an explicit sorted list, for windows at anchors that follow no pattern.\n\nAn N-dimensional Window is a tuple of one AxisWindow per axis, so anisotropy is free: 4 cells along one axis and 32 along another is just two different AxisWindows.","title":"One axis at a time"},{"category":"section","location":"concepts/windows/#Edges","page":"Windows","text":"An extent rarely divides by a window size. Four policies say what to do about the remainder:\n\nPolicy Behaviour\nTruncate() keep only whole windows; the tail is dropped (default)\nPartial() keep the trailing window, clipped, carrying its true smaller count\nCentered() like Truncate, with the remainder split evenly between both ends\nStrict() error unless the windows cover the axis exactly\n\nPadding is deliberately not offered: it would fabricate observations.\n\nPartial is the one to reach for when nothing may be dropped. The counts on a clipped window are real — a mean over a partial tile divides by the cells actually there — so partial windows merge correctly with whole ones. A Partial request on an extent that happens to divide gives exactly the same windows, counts and plan as Truncate.","title":"Edges"},{"category":"section","location":"concepts/windows/#Deriving-one-window-from-another","page":"Windows","text":"Three relations decide what the planner is allowed to do, and each is a pure predicate with an exhaustively tested definition:\n\ncan_coarsen(parent, child) — every child window is k consecutive windows of a tiled parent.\ncan_compose(a, b, child) — every child window is the a window at its origin followed by the b window at origin + a.size.\ncan_restride(parent, child) — the child selects a subset of the parent's windows: a view, free.\n\nThese are the whole legality story. Kernels only ever evaluate merges the predicates have proven aligned, and the tests check every true case against brute-force sets of covered indices on small extents.","title":"Deriving one window from another"},{"category":"section","location":"concepts/windows/#Where-the-output-cells-sit","page":"Windows","text":"geometry(r, key) reports it:\n\ng = geometry(r, (8, 8))\ng.origins     # 0-based origins per axis\ng.ranges      # the input index range each output cell covers\ng.bounds      # physical (low, high) per output cell, when a spacing is known\ng.centers     # physical centres\n\nThat is the answer to \"which cells went into this number\", including under Truncate, where the dropped tail differs per size.","title":"Where the output cells sit"},{"category":"section","location":"performance/#Performance","page":"Performance","text":"","title":"Performance"},{"category":"section","location":"performance/#What-it-costs","page":"Performance","text":"Measured on one core of a Xeon Gold 5418Y, 4096×4096 Float64, as a ratio to a streaming sum(x) in the same process:\n\nRequest Ratio to one sum\nmean, 8×8 tiles 0.82\nmean + variance, 8×8 tiles 1.25\ncovariance of two fields, 8×8 tiles 1.18\nmean, 2×2 tiles 1.17\nsix scales 2…64, mean + variance 2.03\n\nThat last row is the point: six resolutions for about twice the cost of reading the array once, because each level is merged from the level below rather than read from the input. Computing them independently would be about 7.4.\n\nFor requests too large to compare against a single pass, the plan is measured against the same request with no sharing at all — every target from its own base pass:\n\nRequest Planned cost / all-base cost\nsix scales 2…64, mean + variance 0.28\nevery tile size 2…64 0.31\nprime tiles {3,5,7,11,13} × {1,2,4,8} 0.27\nfifteen dense sizes 2…16 0.16\n3-D 512×512×128, fifteen anisotropic tiles 0.25\n\nAnd the executor against its own roofline model — measured time over modelled time — lands between 0.38 and 1.21, so the kernels hit the model or beat it.\n\nThreaded, at 8 threads, against a threaded streaming sum: base pass mean 1.0–1.2, mean + variance 1.9–2.1, covariance 1.4–1.6.\n\n(Image: Cost against the number of scales)","title":"What it costs"},{"category":"section","location":"performance/#Running-the-gates","page":"Performance","text":"julia --project=benchmark benchmark/gates.jl --only=kernels,kernels-f32,planner,executor\njulia -t8 --project=benchmark benchmark/gates.jl --only=threaded\njulia --project=benchmark benchmark/gates.jl --list\n\nEvery gate is a ratio against a reference measured in the same process, so the thresholds hold across machines. Run them on an otherwise idle machine, and run each group in the process it describes: the serial groups measure 20–70 % higher inside a multithreaded process, whose allocator and garbage collector they are not written against.\n\nThe gates are not run in CI. They are throughput ratios, and a shared runner cannot reproduce them — even on a quiet 96-core machine the threaded base-pass ratio moved by 2× between processes until its terms were made long enough to measure. What CI does check is the behaviour: the test suite asserts that every kernel and every prepared request allocates nothing, on every backend.","title":"Running the gates"},{"category":"section","location":"performance/#Why-the-ratios-are-what-they-are","page":"Performance","text":"A base pass reads the input once and writes one accumulator per output cell. At 8×8 tiles the writes are 1/64 of the reads, so the ratio to a pure streaming read is near one; at 2×2 tiles they are 1/4 of the reads and the ratio rises. A coarsen level reads its parent and writes 1/kᴺ as much, so a tower's levels cost a geometric series on top of the base pass — which is why six scales cost 2.03 rather than 6.","title":"Why the ratios are what they are"},{"category":"section","location":"performance/#Fusion-does-not-help-here,-and-was-measured-rather-than-assumed","page":"Performance","text":"The obvious remaining idea is fusion: compute several tiled levels within one cache tile so the intermediate levels never reach DRAM. Counting bytes, that looks like a 33 % saving on the six-scale tower — 402 MB down to 267 MB.\n\nIt was implemented and measured, and it does not pay:\n\nrequest best fused fold vs unfused\n4096², six scales, mean + variance 1.05×\n4096², six scales, mean 1.09×\n4096², every tile size 2…64 1.01×\n512×512×128 Float32, four scales 0.88×\n\nThe byte count is right but the bytes are not equal. The intermediate reads it removes are perfectly sequential and already prefetched — the unfused executor runs at 27.9 GB/s, above the 18.9 GB/s a streaming sum reaches — while the blocking needed to remove them makes the input read worse. Blocking the contiguous axis is 1.8× slower outright; keeping it whole and blocking only the outer axes recovers to about break-even in 2-D. Nothing recovered the 3-D case: coarsening by k shrinks a level by kᴺ, so in three dimensions the intermediates are a seventh of the base pass and there is almost nothing to save, while the blocking cost is paid on the base pass, which is everything.\n\nThe implementation was reverted rather than shipped behind a flag.","title":"Fusion does not help here, and was measured rather than assumed"},{"category":"section","location":"concepts/scales/#Scale-specifications","page":"Scale specifications","text":"A request names a set of windows. The convenient forms come first; everything resolves to the same list.\n\nblockstats(x, 8)                    # one isotropic tile size\nblockstats(x, [2, 4, 8])            # several isotropic sizes\nblockstats(x, [(4, 4, 1)])          # one anisotropic size (a 3-D input, last axis unreduced)\nblockstats(x, [(4, 4, 1), (8, 8, 2)])\n\nFor anything more, build a ScaleSet from a per-axis size generator, a placement, and a rule for combining the axes.","title":"Scale specifications"},{"category":"section","location":"concepts/scales/#Size-generators","page":"Scale specifications","text":"Generator Sizes\nSizes(v) exactly v\nDyadic(; min, max) powers of two in range\nSmooth(primes = (2, 3); min, max) products of those primes in range\nEvery(; min, max) every integer in range\nDivisors(; min, max) sizes that divide the extent\nFixed(k) just k — use Fixed(1) to leave an axis unreduced\nSubsample(gen, budget) at most budget evenly spaced members of gen, endpoints kept\n\nmin and max take an integer, or a Length in the axis' own physical units.","title":"Size generators"},{"category":"section","location":"concepts/scales/#Placement","page":"Scale specifications","text":"Placement Origins\nTiled() every size cells (default)\nStride(k) every k cells\nOverlap(f) every size · (1 - f) cells\nDense() every cell\nAnchors(v) exactly v\nSpread(k) k windows, the first and last flush with the ends","title":"Placement"},{"category":"section","location":"concepts/scales/#Combining-axes","page":"Scale specifications","text":"ScaleSet((Dyadic(max = 64), Dyadic(max = 64), Fixed(1)); combine = Product())\n\nIsotropic() — all axes take the same size. Isotropic((:x, :y)) couples only those two, leaving the rest to vary independently.\nProduct() — the Cartesian product of the per-axis sets.\nZip() — the axes advance together through their sets.\n\nA ScaleSet also takes edge, a filter predicate on the window tuple, include_full to add the window covering the whole extent, and min_elements / max_elements to bound the cells per window.","title":"Combining axes"},{"category":"section","location":"concepts/scales/#Named-axes","page":"Scale specifications","text":"Give the axes names and address them by name:\n\nblockstats(x, ScaleSet((h = Dyadic(max = 64), v = Fixed(1))); dimnames = (:h, :v), stats = (Mean(),))\n\nThe DimensionalData extension takes the names from the array instead.","title":"Named axes"},{"category":"section","location":"migration/#Migrating-from-0.1","page":"Migrating from 0.1","text":"Version 0.2 is a ground-up rebuild. Nothing from 0.1 is deprecated — it is gone — so this note maps the old surface onto the new one. The concepts survived; the names, the layering and the planner did not.","title":"Migrating from 0.1"},{"category":"section","location":"migration/#Entry-points","page":"Migrating from 0.1","text":"0.1 0.2\nreduce_stats(x, scales; stats, backend) blockstats(x, scales; stats, backend)\nreduce_stats(x, y, scales; stats) blockstats((x = x, y = y), scales; stats = (Cov(:x, :y),))\nprepare(x, scales; …) → PreparedReduction prepare(x, scales; …) → Prepared\nreduce_stats!(p, x) blockstats!(p, x)\nmaterialize(result) / materialize! not needed — results are already arrays\nMultiResResult ScaleResults\nfactors(r) scales(r)\nshapes(r) shapes(r)\nstat_name(tag) name the statistics yourself: stats = (mean_u = Mean(:u),)\n\nScaleResults indexes by size tuple as before (r[(8, 8)]), and also by the full window and by position. It adds windows(r), statnames(r), geometry(r, key), dimnames(r) and spacing(r).","title":"Entry points"},{"category":"section","location":"migration/#Asking-for-scales","page":"Migrating from 0.1","text":"0.1 had three separate mechanisms. 0.2 has one: every request resolves to a list of windows.\n\n0.1 0.2\n[2, 4, 8] (isotropic factors) [2, 4, 8] — unchanged\nTower(; base_factor = 2, steps = [2], maxfactor = 64) Dyadic(; min = 2, max = 64), or just [2, 4, 8, 16, 32, 64]\nLadder / scale_ladder(…) ScaleSet(Smooth((2, 3)); …), or Subsample(gen, budget) to thin any generator\nSliding(w; stride, origin) ScaleSet(Sizes([w]); placement = Stride(stride))\n— placement = Tiled() / Stride(k) / Overlap(frac) / Dense() / Anchors(v) / Spread(k)\n— edge = Truncate() / Partial() / Centered() / Strict()\n— sizes in physical units: Dyadic(; max = Length(500.0)) with spacing\n\nA sliding window is no longer a separate code path: tiles are the stride == size case of the same geometry, and the planner spans both from one derivation graph.","title":"Asking for scales"},{"category":"section","location":"migration/#Statistics","page":"Migrating from 0.1","text":"The tags kept their names — Count, Sum, Mean, Var, Std, Cov, Min, Max, Moments — and gained Extrema, Corr, ProductMean, CentralMoments, Skewness, Kurtosis and Component.\n\nComponent(tag, field) replaces the custom tags 0.1 users wrote to reach raw accumulator fields: Component(Var(), :M2) is the variance numerator, Component(Cov(:u, :w), :C) the covariance numerator.\n\nStatistics bind fields by name or position, so one pass computes Var(:u), Var(:w) and Cov(:u, :w) together instead of three passes.","title":"Statistics"},{"category":"section","location":"migration/#Accumulator-interface","page":"Migrating from 0.1","text":"0.1 0.2\nempty_acc(A) neutral(A)\ninverse_merge(ab, b) unmerge(ab, b)\nresult_value(tag, a, T) finalize(tag, a, T)\ndefault_output_eltype(tag, T) result_eltype(tag, T)\nstat_arity(tag) / arity(A) arity(A)\nCompositeAccumulator{Tuple{…}} Composite{Members,Bindings}\nArray{Acc,N} storage AccumulatorArray (struct-of-arrays; constant components cost no memory)\n\nNew in the interface: the two-phase combine protocol (phases, p1lift, mid, p2lift, finish) that lets a composite of any number of members cost at most two passes over a box, and unshift / shiftable for shifted accumulation.","title":"Accumulator interface"},{"category":"section","location":"migration/#Backends","page":"Migrating from 0.1","text":"0.1 defined its own backend types. 0.2 dispatches on ComputationalBackends.jl:\n\nusing ComputationalBackends: ComputationalBackends as CB\nblockstats(x, [8]; stats = (Mean(),), backend = CB.ThreadedBackend())\n\nCB.AutoBackend() is the default and picks a GPU backend for device arrays, threads when more than one is available, else serial.\n\nMPI changed shape: 0.1 had every rank hold the whole input and Allgatherv! the results. 0.2 takes a partitioned tensor — each rank passes its own slab as Partitioned(slab; axis = d) — and results stay partitioned by owning rank.","title":"Backends"},{"category":"section","location":"migration/#Things-that-no-longer-exist","page":"Migrating from 0.1","text":"ReductionPlan / ReductionStep / tower_plan / solver_plan: the planner is plan(shape, targets; …) and produces a Plan of derivations (Base_, Coarsen, Compose, Restride, Scan).\nplan_work / naive_work / n_base_passes / total_work: explain(p) reports passes, bytes, merges and peak memory.\nTowerBuffers / allocate_tower: Workspace with liveness-based buffer reuse.\nsliding_reduce: sliding windows go through the same blockstats as everything else.","title":"Things that no longer exist"},{"category":"section","location":"concepts/numerics/#Numerics","page":"Numerics","text":"","title":"Numerics"},{"category":"section","location":"concepts/numerics/#What-is-computed","page":"Numerics","text":"Single observations enter through a Welford lift.\nTwo accumulators combine with the Chan–Golub–LeVeque merge for (n, mean, M2), and Pébay's forms for covariance and central moments to order four.\nCombining many at once uses the weighted-mean form — mean = Σnᵢμᵢ / Σnᵢ, M2 = ΣM2ᵢ + Σnᵢ(μᵢ - μ)² — never Σx² - (Σx)²/n.\nA base tile's second moments are computed in two passes over the box: the mean, then the centred sum. The box is in cache, so this is two passes over registers.\n\nThat combination gives error O(ε‖x - μ‖²) within a tile, which is the best attainable without compensated summation.","title":"What is computed"},{"category":"section","location":"concepts/numerics/#The-limit-that-remains","page":"Numerics","text":"Merging is not the weak link. Even with a perfect tree, the running means are stored at the data's own magnitude, so a relative M2 error of about 0.3 · ulp(|mean|) / scale is inherent. Measured with n = 2×10⁴, spread 10⁻³:\n\noffset relative M2 error\n0 ~1e-16\n1e4 ≲ 1e-9\n1e8 3e-6 to 1e-5\n\nSequential and pairwise merging give the same figure; tree order is for parallelism, not accuracy.","title":"The limit that remains"},{"category":"section","location":"concepts/numerics/#Shifted-accumulation","page":"Numerics","text":"The fix is to accumulate statistics of x - s for one shift s per field per call. Central moments are shift-invariant and means simply move by s, so unshift restores the reported values at finalize; by Sterbenz the subtraction itself is exact when s is near the data.\n\nThis is what lets a Float32 input accumulate in Float32. Measured per-tile variance against a BigFloat reference, 8×8 tiles, spread 10⁻³:\n\nmean / std Float64 accumulation Float32, unshifted Float32, shifted\n0 4e-16 2.1e-7 1.8e-7\n1e5 0 2.4e-4 2.5e-7\n1e7 0 0.178 5.8e-8\n\n(Image: Float32 accumulation with and without the shift)\n\nThe shift is global rather than per tile, so all accumulators still compose, and it is recomputed on every call from a strided sample of at most SHIFT_SAMPLES elements — a prepared handle re-centres on each new input.\n\nshift = :auto (the default) shifts exactly where it buys something: a floating input whose accumulation would otherwise have to widen. Float64 input is already well conditioned and is left alone. shift = true asks for it regardless, false refuses, and a number or tuple pins it.\n\nAccumulators of raw, uncentered moments (SumAcc, ProductSumAcc, RawMomentsAcc) report shiftable = false: un-shifting them would need quantities they do not carry, and they are well conditioned without it.","title":"Shifted accumulation"},{"category":"section","location":"concepts/numerics/#Element-types","page":"Numerics","text":"accumulation_eltype widens Float16 → Float32, Float32 → Float64, integers and Bool → Float64, and leaves wider floats alone — but only when the request is not shifting. With a shift, a Float32 input accumulates in Float32, which is both faster and, at a large offset, far more accurate.\n\nResults narrow exactly once, at finalize, to result_eltype — the input eltype for sums and extremes, a float for means, moments and correlations. acc_eltype and out_eltype override both.","title":"Element types"},{"category":"section","location":"concepts/numerics/#Windows-with-nothing-in-them","page":"Numerics","text":"A window can hold no observations once non-finite ones are skipped. Its mean and variance are then NaN, not zero: the corrected denominator is clamped at zero so the division is 0/0, and the mean reports NaN directly. A sum of no observations is 0, which is correct.","title":"Windows with nothing in them"},{"category":"section","location":"concepts/statistics/#Statistics","page":"Statistics","text":"","title":"Statistics"},{"category":"section","location":"concepts/statistics/#Accumulators","page":"Statistics","text":"Every statistic is computed from an accumulator: an immutable, isbits struct holding sufficient statistics for a set of observations, together with a way to combine two of them.\n\nneutral(A)          # the identity: merge(neutral(A), a) == a\nlift(A, xs)         # the accumulator of one observation\nmerge(a, b)         # associative and commutative\n\nA variance accumulator is VarAcc(n, mean, M2). Merging two of them is the Chan–Golub–LeVeque update — no observation is revisited. That single property is what makes everything else possible: a tile of 64 cells is the merge of two tiles of 32, and the planner never has to touch the input twice.\n\nSome accumulators also have a group inverse (unmerge), reported by is_invertible. It exists for callers who want it; the planner never uses it, because subtracting accumulators is the cancellation-prone form the numerics policy rules out.","title":"Accumulators"},{"category":"section","location":"concepts/statistics/#The-statistics-that-ship","page":"Statistics","text":"Tag Reads Reports\nCount() one field number of observations\nSum() one field Σx\nMean() one field x̄\nVar(; corrected) / Std(; corrected) one field variance / standard deviation\nMin() / Max() / Extrema() one field extremes\nMoments(K) one field ⟨x⟩ … ⟨xᴷ⟩\nCentralMoments(K) (K ≤ 4) one field central moments 2…K\nSkewness() / Kurtosis(; excess) one field shape\nCov(i, j; corrected) two fields covariance\nCorr(i, j) two fields correlation\nProductMean(i, j) two fields ⟨xy⟩\nComponent(tag, field) as tag one raw accumulator field\n\ncorrected takes true (the default, dividing by n - 1), false (dividing by n), or — for weighted requests — :frequency and :reliability. See weights.\n\nComponent is the escape hatch for the numerators themselves: Component(Var(), :M2) is Σ(x - x̄)² and Component(Cov(:u, :w), :C) is Σ(u - ū)(w - w̄), which is what you want when the results will be merged again later by something outside this package.","title":"The statistics that ship"},{"category":"section","location":"concepts/statistics/#Binding-fields","page":"Statistics","text":"A statistic reads one or more input fields, given by position or by name:\n\nr = blockstats((u = u, w = w), [8];\n               stats = (var_u = Var(:u), var_w = Var(:w), flux = Cov(:u, :w)))\n\nThis is one pass over u and w, not three. assemble works out the distinct accumulators the request needs, drops any that another subsumes (a Mean(:u) next to a Var(:u) costs nothing extra, because VarAcc already carries the mean), and builds a single composite accumulator. The composite implements the same interface member-wise, with generated code, so every member's field index is a compile-time constant.\n\nName the results by passing a NamedTuple, as above; otherwise each tag names itself (:mean, :var, :cov_u_w) and two tags that would collide are an error.","title":"Binding fields"},{"category":"section","location":"concepts/statistics/#Two-passes-at-most,-whatever-you-ask-for","page":"Statistics","text":"A second moment is most accurate when computed in two passes — the mean first, then the centred sum. The accumulators expose that as a protocol (phases, p1lift, mid, p2lift, finish) rather than as a special case, and a composite fuses the phases of all its members. So a request for mean, variance, minimum, maximum and a covariance costs at most two passes over each box, never one pass per statistic.\n\nBoxes are small enough to stay in cache, so those two passes are two passes over registers, not over memory.","title":"Two passes at most, whatever you ask for"},{"category":"section","location":"concepts/statistics/#Writing-your-own","page":"Statistics","text":"Implement neutral, lift and Base.merge for an AbstractAccumulator, plus acc_eltype and arity if it reads more than one field; then a tag with bindings, accumulator_type, name, result_eltype and finalize. check_monoid(A; samples) verifies the laws — identity, commutativity, associativity, that grouping does not change the result, and that combine agrees with a fold.","title":"Writing your own"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Requests","page":"API reference","text":"","title":"Requests"},{"category":"section","location":"api/#Results","page":"API reference","text":"","title":"Results"},{"category":"section","location":"api/#Scale-specifications","page":"API reference","text":"","title":"Scale specifications"},{"category":"section","location":"api/#Windows","page":"API reference","text":"","title":"Windows"},{"category":"section","location":"api/#Coordinates","page":"API reference","text":"","title":"Coordinates"},{"category":"section","location":"api/#Statistics","page":"API reference","text":"","title":"Statistics"},{"category":"section","location":"api/#Weights","page":"API reference","text":"","title":"Weights"},{"category":"section","location":"api/#Planning-and-execution","page":"API reference","text":"","title":"Planning and execution"},{"category":"section","location":"api/#Partitioned-tensors","page":"API reference","text":"","title":"Partitioned tensors"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.blockstats","page":"API reference","text":"blockstats(fields, scales; stats, kwargs...) -> ScaleResults\n\nStatistics of fields over every window scales asks for, computed in one shared plan. fields is one array or a NamedTuple/Tuple of same-shaped arrays; statistics bound to different fields (a covariance of two of them, say) are still produced in a single pass. Keywords are prepare's.\n\nr = blockstats(x, [2, 4, 8]; stats = (Mean(), Var()))\nr[(4, 4)].mean\n\nr = blockstats((u = u, w = w), [8]; stats = (Var(:u), Var(:w), Cov(:u, :w)))\nr[(8, 8)].cov_u_w\n\nFor many inputs of the same shape, prepare once and call blockstats!.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.blockstats"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.prepare","page":"API reference","text":"prepare(fields, scales; stats, edge = Truncate(), backend = CB.AutoBackend(),\n        acc_eltype = nothing, out_eltype = nothing, weights = nothing, skipnan = false,\n        memory_limit = typemax(Int)) -> Prepared\n\nResolve a request against inputs shaped like fields (one array, or a NamedTuple/Tuple of same-shaped arrays) and allocate everything it needs. scales is anything resolve accepts. stats is a tuple of statistic tags; a tag bound to a field name or position selects which input it reads. Accumulation defaults to accumulation_eltype of the promoted input eltype and results to each tag's result_eltype. dimnames names the axes, so a scale specification may address them by name; spacing gives their coordinates, so sizes may be given as a Length and results report the physical extent of every output cell.\n\nweights weights every statistic of the request: an array of one weight per input element, or per-axis factors as a tuple with one entry per axis (nothing where an axis has no factor) or a NamedTuple keyed by dimnames, whose product is formed per element. A statistic with no weighted form throws; Count() stays the number of observations. Var, Std and Cov take corrected = :frequency | :reliability | false alongside true for the weighted denominators.\n\nskipnan = true drops non-finite observations: each statistic sees only the elements whose own bound fields are all finite, and the counts and total weights reflect that.\n\ninto writes the results into arrays the caller already holds — one NamedTuple of arrays per requested window, shaped and named like the ones prepare would otherwise allocate — instead of allocating them.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.prepare"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.blockstats!","page":"API reference","text":"blockstats!(p::Prepared, fields) -> ScaleResults\n\nRun the prepared request on new input. Allocation-free at steady state. The returned result aliases p's arrays and is overwritten by the next call — copy what you need before reusing p.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.blockstats!"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.prepare_on","page":"API reference","text":"prepare_on(fields, scales, backend; kwargs...) -> Prepared\n\nprepare with the backend already concrete. A backend that does not run the kernels in this process defines its own method; the fallback here reports the extension that would have defined it.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.prepare_on"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.release!","page":"API reference","text":"release!(p) -> nothing\n\nGive back whatever a prepared request holds outside this process. A request whose work is all local holds nothing, so this does nothing.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.release!"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.explain","page":"API reference","text":"explain([io,] plan; in_bytes = 8, acc_bytes = 24)\n\nPrint every node (sizes, positions, shape, derivation) and the plan's totals: passes over the input, bytes moved, merges, peak storage.\n\n\n\n\n\nPlan report for a prepared request or a result (see explain on a plan).\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.explain"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.ScaleResults","page":"API reference","text":"ScaleResults{N}\n\nResults of one request: for every requested window, a NamedTuple of arrays keyed by statistic name. Index by window, by size tuple when that is unambiguous, or by position; iterate windows / scales.\n\nr[(8, 8)]              # NamedTuple of arrays at tile size 8×8\nr[(8, 8)].mean         # one array\nscales(r)              # the requested sizes, coarsest last\ngeometry(r, (8, 8))    # window, per-axis origins and covered ranges\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.ScaleResults"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.windows","page":"API reference","text":"Requested windows, in the order the request produced them.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.windows"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.scales","page":"API reference","text":"Requested window sizes, aligned with windows.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.scales"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.geometry","page":"API reference","text":"geometry(r::ScaleResults, key) -> NamedTuple\n\nWhere the windows of one result sit: the window, the axis names, the per-axis origins (0-based), the input index ranges each output cell covers, and — when the axis had a coordinate spacing — the physical bounds and centers of every output cell.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.geometry"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.statnames","page":"API reference","text":"Names of the statistics in each result.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.statnames"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.dimnames","page":"API reference","text":"Axis names of the input, or nothing when it had none.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.dimnames"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.spacing","page":"API reference","text":"Per-axis coordinate spacing of the input, or nothing where an axis had none.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.spacing"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.rebuild","page":"API reference","text":"rebuild(r::ScaleResults, results) -> ScaleResults\n\nThe same request with each window's statistics replaced by results[i], keeping the windows, plan, names and spacing. Extensions use this to present results in their own container.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.rebuild"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.ScaleSet","page":"API reference","text":"ScaleSet(axes; combine = Isotropic(), placement = Tiled(), edge = Truncate(), filter = Returns(true),\n         include_full = false, min_elements = 1, max_elements = typemax(Int))\n\nWhich windows to compute. axes is one generator for every axis, a tuple of generators (one per axis) or a NamedTuple keyed by axis name (unnamed axes are Fixed(1)); an Integer stands for Fixed, a vector of integers for Sizes. placement is one Placement or a tuple with one per axis. filter receives each candidate Window.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.ScaleSet"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.resolve","page":"API reference","text":"resolve(spec, shape::NTuple{N,Int}; spacing = nothing, dimnames = nothing) -> Vector{Window{N}}\n\nWindows requested by spec on an array of shape, deduplicated and sorted by volume. spec is a ScaleSet, a vector of isotropic sizes, a vector of per-axis size tuples, a single size or size tuple, or windows. spacing (one AxisSpacing or nothing per axis) resolves Length bounds; dimnames matches a NamedTuple of generators to axes.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.resolve"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Resolved","page":"API reference","text":"Resolved(windows)\n\nTarget windows worked out by the caller. resolve returns them in the given order and keeps every one of them, so a caller that has to line its windows up with something else keeps its own ordering. The windows must be distinct, since a plan produces each of its targets once.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Resolved"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.AxisWindow","page":"API reference","text":"AxisWindow(extent, size, pos::Positions, partial::Bool)\n\nWindows of size cells at origins pos along an axis of extent cells. With partial == false every window lies inside the axis; with partial == true the trailing windows may be clipped at the extent.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.AxisWindow"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Origins","page":"API reference","text":"Origins(origins::AbstractVector{<:Integer})\n\nAn explicit strictly increasing list of origins. Parametric in the vector type so a window can be moved to a device alongside the data it addresses; the checked constructor builds the host form.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Origins"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.tiled","page":"API reference","text":"Tiling of an axis of extent cells by windows of size cells under an edge policy.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.tiled"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.strided","page":"API reference","text":"Windows of size placed every stride cells from the axis start under an edge policy.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.strided"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.anchored","page":"API reference","text":"Windows of size at explicit origins.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.anchored"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.can_coarsen","page":"API reference","text":"true when every child window is the union of k consecutive windows of the tiled parent.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.can_coarsen"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.can_compose","page":"API reference","text":"true when every child window is the window of a at the same origin followed by the window of b at origin + a.size.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.can_compose"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.can_restride","page":"API reference","text":"true when child selects a subset of parent's windows (same extent and size).\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.can_restride"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Regular","page":"API reference","text":"Cells of equal physical step; the low edge of the first cell is at first.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Regular"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Edges","page":"API reference","text":"Cell edges (extent + 1 increasing values).\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Edges"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Length","page":"API reference","text":"A size given in physical units of an axis' coordinates.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Length"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.spacing_from_points","page":"API reference","text":"spacing_from_points(centers::AbstractVector) -> AxisSpacing\n\nThe spacing of an axis whose coordinates are the cell centres centers. Evenly spaced centres give a Regular spacing; otherwise the edges are the midpoints between neighbours, with the outermost two reflected so the first and last cells keep their own width. One centre gives a unit-wide cell.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.spacing_from_points"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.AbstractAccumulator","page":"API reference","text":"AbstractAccumulator\n\nSufficient statistics of a set of observations. Concrete subtypes are immutable isbits structs with the default positional constructor and implement neutral, lift and Base.merge.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.AbstractAccumulator"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.AbstractStatistic","page":"API reference","text":"AbstractStatistic\n\nA requested statistic. A tag selects an accumulator type (accumulator_type), reads input fields (bindings) and turns one accumulator into a value (finalize).\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.AbstractStatistic"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.neutral","page":"API reference","text":"neutral(::Type{A}) -> A\n\nMonoid identity: merge(neutral(A), a) == a == merge(a, neutral(A)).\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.neutral"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.lift","page":"API reference","text":"lift(::Type{A}, xs::Tuple) -> A\n\nAccumulator of one observation; xs holds the arity(A) bound raw values in binding order.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.lift"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.bindings","page":"API reference","text":"Input fields a tag reads, as positions or names, in the order its accumulator binds them.\n\n\n\n\n\nPer-member input-field positions of a composite type.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.bindings"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.accumulator_type","page":"API reference","text":"accumulator_type(tag, ::Type{Tin}, ::Type{Tacc}) -> Type{<:AbstractAccumulator}\n\nAccumulator a tag materializes for input eltype Tin accumulating in Tacc.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.accumulator_type"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.result_eltype","page":"API reference","text":"Default output eltype of a tag for input eltype Tin.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.result_eltype"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.name","page":"API reference","text":"Result key of a tag; field names are appended when the binding is not the default.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.name"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.arity","page":"API reference","text":"Number of input fields one observation of the accumulator binds.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.arity"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.acc_eltype","page":"API reference","text":"Element type of the accumulator's moment fields.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.acc_eltype"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.is_invertible","page":"API reference","text":"true when unmerge is defined for the accumulator type.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.is_invertible"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.subsumes","page":"API reference","text":"true when an A can finalize every statistic a B can, for identical bindings.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.subsumes"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.phases","page":"API reference","text":"Number of passes over the children combine needs: 1 or 2.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.phases"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.p1init","page":"API reference","text":"Phase-1 state before any child.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.p1init"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.p1lift","page":"API reference","text":"Phase-1 contribution of one child.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.p1lift"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.p1merge","page":"API reference","text":"Associative combination of two phase-1 states.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.p1merge"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.mid","page":"API reference","text":"Quantities phase 2 needs, from the completed phase-1 state.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.mid"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.p2init","page":"API reference","text":"Phase-2 state before any child.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.p2init"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.p2lift","page":"API reference","text":"Phase-2 contribution of one child given the phase-1 quantities m.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.p2lift"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.p2merge","page":"API reference","text":"Associative combination of two phase-2 states.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.p2merge"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.finish","page":"API reference","text":"The accumulator from the phase states (m and s2 are nothing for single-phase types).\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.finish"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.lift_skipping","page":"API reference","text":"lift_skipping(::Type{A}, xs::Tuple) -> A\n\nlift of an observation that may not be finite: neutral(A) unless every bound value is finite. A composite skips member by member, so a gap in one field does not drop the others.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.lift_skipping"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.combine","page":"API reference","text":"combine(::Type{A}, children) -> A\n\nCombine an iterable of accumulators; the k-ary counterpart of merge(a::A, b::A). Runs the two-phase protocol: phase 1 folds p1lift with p1merge, mid derives what phase 2 needs, phase 2 folds p2lift with p2merge, and finish builds the result. Kernels run the same protocol over boxes and fuse the phases of all members of a composite.\n\n\n\n\n\ncombine(::Type{A}, b::Box) -> A\n\nThe two-phase protocol over the leaves of a box; for composites every member's phases run fused.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.combine"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.unmerge","page":"API reference","text":"unmerge(ab::A, b::A) -> A\n\nGroup inverse: the a with merge(a, b) == ab. Defined only when is_invertible.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.unmerge"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.finalize","page":"API reference","text":"finalize(tag, a::AbstractAccumulator, ::Type{Tout}) -> Tout\n\nValue of the statistic for one accumulator. Methods exist for every accumulator that can serve the tag.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.finalize"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.unshift","page":"API reference","text":"unshift(a::A, shifts::Tuple) -> A\n\nThe accumulator a would have been, had its observations not been shifted by shifts (one per bound field). Statistics of shifted data are accumulated so that a mean-sized offset never enters a difference: central moments are shift-invariant, means move by the shift.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.unshift"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.shiftable","page":"API reference","text":"shiftable(::Type{A}) -> Bool\n\ntrue when A has an unshift, so its observations may be shifted before accumulating. False for accumulators of raw (uncentered) moments, whose un-shifting needs quantities they do not carry — they are also well conditioned without a shift, so they lose nothing.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.shiftable"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.accumulation_eltype","page":"API reference","text":"accumulation_eltype(::Type{Tin}) -> Type\n\nElement type in which moments accumulate for input eltype Tin: Float16 → Float32, Float32 → Float64, integers and Bool → Float64, other floats unchanged.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.accumulation_eltype"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.ratio_eltype","page":"API reference","text":"ratio_eltype(::Type{Tin}) -> Type\n\nOutput eltype for statistics whose value is generally not representable in Tin (means, moments, correlations): the input eltype when it is already floating point, else the accumulation eltype.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.ratio_eltype"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.check_monoid","page":"API reference","text":"check_monoid(::Type{A}; samples, rtol = 1e-8) -> Bool\n\nVerify identity, commutativity, associativity, fold/k-ary agreement and, when claimed, the group inverse for accumulator type A on samples (raw observations; tuples for arity > 1). Throws on the first violated law.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.check_monoid"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Composite","page":"API reference","text":"Composite{M<:Tuple,B}(members::M)\n\nProduct of member accumulators over one observation of several input fields. B is a tuple with one NTuple{K,Int} per member naming the input-field positions that member binds. Satisfies the whole accumulator interface member-wise; its two-phase protocol fuses the phases of all members, so a composite costs at most two passes over its children.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Composite"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.assemble","page":"API reference","text":"assemble(stats, fieldnames::Tuple, ::Type{Tin}, ::Type{Tacc})\n    -> (CompositeType, routing::Tuple{Vararg{Val}}, names::Tuple{Vararg{Symbol}}, result_eltypes::Tuple)\n\nComposite accumulator type serving every tag in stats over input fields fieldnames, with each tag routed (by Val member index) to the member that finalizes it. Members with identical bindings that another member subsumes are dropped. A NamedTuple of tags takes its result names from the keys, a Tuple from each tag's own name.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.assemble"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Weighted","page":"API reference","text":"Weighted(stat, W)\n\nstat over weighted observations, binding input field W as the weight after the fields stat binds itself. The weights keyword of prepare builds these, binding the weight source it resolves.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Weighted"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.weighted_accumulator","page":"API reference","text":"weighted_accumulator(stat, ::Type{Tacc}) -> Type{<:AbstractAccumulator}\n\nAccumulator the weighted form of stat uses. Statistics whose weighted form is not defined throw here, so a weighted request never silently computes an unweighted quantity.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.weighted_accumulator"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.weight_stat","page":"API reference","text":"weight_stat(stat, W::Int) -> AbstractStatistic\n\nstat rewritten to read input field W as a weight. Count is returned unchanged: the number of observations in a window does not depend on their weights.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.weight_stat"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.weight_source","page":"API reference","text":"weight_source(weights, shape, dimnames) -> Union{Nothing,WeightSource}\n\nThe weight field a weights keyword asks for: nothing, an ElementWeights of an array of one weight per input element, or SeparableWeights of per-axis factors given as a tuple (one entry per axis, nothing where an axis has no factor) or as a NamedTuple keyed by axis name.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.weight_source"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.weight_factors","page":"API reference","text":"weight_factors(weights, ::Val{N}, dimnames) -> NTuple{N,Any}\n\nPer-axis weight factors as one entry per axis, nothing where an axis has none. A NamedTuple is matched to dimnames; a tuple is already in that form.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.weight_factors"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.WeightSource","page":"API reference","text":"WeightSource\n\nAn input field of observation weights. A weight is a field in its own right so that the kernels read it through the same path as the data, and its own type so that a shift is never subtracted from it.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.WeightSource"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.ElementWeights","page":"API reference","text":"Weights given one per input element.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.ElementWeights"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.SeparableWeights","page":"API reference","text":"SeparableWeights(factors, dims)\n\nWeights that are the product of one factor per axis over an input of shape dims; nothing in place of a factor leaves that axis out of the product. The product is formed per element, so an N-D weight array is never materialized.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.SeparableWeights"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Skipping","page":"API reference","text":"Skipping(fields)\n\nInput fields whose non-finite observations contribute nothing: a leaf lifts to neutral for every accumulator whose own bound values are not all finite, so one field's gaps do not drop the others.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Skipping"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.plan","page":"API reference","text":"plan(shape, targets::Vector{Window{N}}; backend, in_bytes, acc_bytes, memory_limit, chains) -> Plan{N}\n\nDerivation DAG producing every target window from raw fields of shape at minimal roofline cost under kernel_limits(backend, N). in_bytes is the byte size of one observation over all fields and acc_bytes the storage per accumulator cell. Dense doubling chains are streamed candidates (chains); when the peak live storage would exceed memory_limit they are dropped and the plan is rebuilt.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.plan"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Plan","page":"API reference","text":"Plan{N}\n\nNodes with one derivation each, an execution order (views excluded), a buffer id per node (0 for views), and the requested nodes as outputs, in target order.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Plan"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.allocate","page":"API reference","text":"allocate(plan, ::Type{A}, prototype::AbstractArray; uniform_counts = true) -> Workspace\n\nAllocate the plan's buffers for accumulator type A where prototype lives, build the restride views, and prepare one step per computed node. uniform_counts = false stores a count per cell even where the windows all hold the same number of them, as a request that drops observations needs.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.allocate"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.run!","page":"API reference","text":"run!(ws::Workspace, plan::Plan, fields, backend) -> ws\n\nExecute every computed node of plan in order into the workspace; fields are the raw input arrays in binding order, or a Skipping of them. Allocation-free once the steps are compiled.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.run!"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.boxfold!","page":"API reference","text":"boxfold!(out, src, w::Window, [shape::Val,] backend) -> out\n\nFill every cell I of out with the combination of the leaves of src inside window w at I. src is a field array, a tuple or NamedTuple of field arrays (base pass: leaves are lifted observations), or an AccumulatorArray (coarsen: leaves are its cells, w addressed in its cell grid). shape is Val(static_shape(w)); without it, it is computed and dispatched on once per call.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.boxfold!"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.compose!","page":"API reference","text":"compose!(out, a, b, axis, amap, bmap, backend) -> out\n\nout[I] = merge(a[Ia], b[Ib]) where Ia/Ib are I with the axis index replaced by amap[I[axis]]/bmap[I[axis]]; the other axes must match between out, a and b.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.compose!"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.scan!","page":"API reference","text":"scan!(out, src, axis, size, partial, scratch, backend) -> out\n\nDense sliding windows of size cells along axis: out cell j is the combination of src cells j : j+size-1 along axis. With partial, out has the extent of src and trailing windows are clipped; otherwise it has extent - size + 1 cells along axis. Any monoid; no inverse is used.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.scan!"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.finalize!","page":"API reference","text":"finalize!(dst, src::AccumulatorArray, tag, [shifts,] backend) -> dst\n\nWrite finalize(tag, src[i], eltype(dst)) into every cell of dst, undoing shifts (one per field the accumulator binds, zero by default) first. Select a composite member with member_array first.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.finalize!"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.AccumulatorArray","page":"API reference","text":"AccumulatorArray{A,N,C}(components::C, dims)\n\nStruct-of-arrays storage for accumulators of type A: one component array (or Uniform) per leaf field, nested as a NamedTuple mirroring the accumulator's fields; tuple fields (composite members, power sums) appear as m1, m2, …. Indexing rebuilds the accumulator; writing scatters its fields.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.AccumulatorArray"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Uniform","page":"API reference","text":"A component whose value is the same in every cell; occupies no memory and ignores writes.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Uniform"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.member_array","page":"API reference","text":"Zero-copy storage of member k of an array of composites.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.member_array"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Shifted","page":"API reference","text":"Shifted(field, shift)\n\nAn input field read as field[J] - shift. Accumulating shifted observations keeps a large mean out of every difference the moment kernels take, so the accumulation eltype only has to resolve the spread of the data rather than its offset. unshift puts the offset back at finalize.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Shifted"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Partitioned","page":"API reference","text":"Partitioned(fields; axis)\n\nOne rank's share of a tensor split along axis: the global tensor is the concatenation of every rank's fields along that axis, in rank order. fields is an array or a NamedTuple/Tuple of same-shaped arrays, as prepare takes.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Partitioned"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.Slab","page":"API reference","text":"Slab(axis, offset, extent, global_extent)\n\nOne share of a tensor partitioned along axis: extent cells starting offset cells into an axis of global_extent cells.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.Slab"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.partition","page":"API reference","text":"partition(targets, s::Slab) -> NamedTuple\n\nHow a slab divides a resolved request. Returns the owned windows in global coordinates, the interior windows in slab-local coordinates, the boundary windows in staging-local coordinates, the 0-based global cell staging range the boundary windows read, and the number of interior windows split in each target — the point along the partitioned axis where each output array changes hands.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.partition"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.split_origins","page":"API reference","text":"split_origins(aw::AxisWindow, s::Slab) -> (interior, boundary)\n\nGlobal origins of aw whose first cell lies in s, as those whose window also ends inside s and those that reach past it. window_stop grows with the origin, so the interior ones come first.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.split_origins"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.slab_request","page":"API reference","text":"slab_request(part, fields, stagefields, slab, global_shape; weights, stageweights, dimnames, spacing, kwargs...)\n\nSplit a resolved request between a rank's own slab and its staging region, given the partition the caller already computed to size the staging arrays. fields are the rank's local arrays and stagefields the same fields over part.staging, both in the container form prepare takes; the remaining keywords are prepare's. Element weights are sliced like the fields, so weights covers the slab and stageweights the staging region.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.slab_request"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.run_slab!","page":"API reference","text":"run_slab!(sr::SlabRequest, fields) -> ScaleResults\n\nRun both plans of a split request. stagefields must already hold the cells the boundary windows read; filling them is the distributed backend's job.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.run_slab!"},{"category":"type","location":"api/#BlockwiseStatisticalReductions.SlabRequest","page":"API reference","text":"SlabRequest\n\nA partitioned request as this rank runs it: the plan over its own slab, the plan over the staging region its boundary windows read, one set of result arrays the two write disjoint slices of, and the presented ScaleResults whose windows are in global coordinates.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.SlabRequest"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.stagefields","page":"API reference","text":"The staging arrays the boundary plan reads; a distributed backend fills these before every run.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.stagefields"},{"category":"function","location":"api/#BlockwiseStatisticalReductions.axis_slice","page":"API reference","text":"A view of a holding only the cells r (1-based) along axis.\n\n\n\n\n\n","title":"BlockwiseStatisticalReductions.axis_slice"},{"category":"section","location":"concepts/planner/#The-planner","page":"The planner","text":"The planner is the reason this package exists. Given a set of target windows, it finds a cheap way to produce all of them together.\n\n(Image: The plan for five tile sizes, against one pass each)","title":"The planner"},{"category":"section","location":"concepts/planner/#Nodes-and-derivations","page":"The planner","text":"A node is a window. An edge says how that node is computed:\n\nDerivation Meaning Cost\nBase_ reduce raw input cells inside every window reads the input\nCoarsen merge k consecutive windows of a tiled parent, per axis reads the parent\nCompose merge parent a at each origin with parent b at origin + a.size reads two parents\nRestride select a subset of a parent's windows free — it is a view\nScan dense sliding windows along an axis, two-stack reads the parent once\n\nEvery edge is legal exactly when the matching predicate from windows holds on every axis, so a plan never contains a merge that has not been proven aligned.","title":"Nodes and derivations"},{"category":"section","location":"concepts/planner/#Why-it-spans-every-size","page":"The planner","text":"Two facts do all the work:\n\nDoubling. A dense chain D₀ = lift(x), Dⱼ₊₁[i] = Dⱼ[i] ⊕ Dⱼ[i + 2ʲ] costs one merge per cell per level and gives every power-of-two window size.\nComposition. A window of size s₁ + s₂ is the size-s₁ window merged with the size-s₂ window shifted by s₁, whenever s₁ is a multiple of the stride so the positions stay on the grid.\n\nTogether: window size 12 is D₃ ⊕ D₂ shifted by 8 — one merge per output. Size 13 is two. Any size costs popcount(size) - 1 merges per output from the chain, and a tile of that size is a free Restride of the dense node.\n\nDivisor coarsening remains the cheapest special case and the planner prefers it where it applies, because a coarsened parent has far fewer cells than a dense one.","title":"Why it spans every size"},{"category":"section","location":"concepts/planner/#Choosing","page":"The planner","text":"Cost is roofline: max(bytes / bandwidth, merges / merge_rate, serial_merges / serial_merge_rate), with the rates coming from kernel_limits(backend, N). The search is greedy Steiner — repeatedly add the candidate intermediate that lowers total cost the most — over candidates built from the gcd-closure of the tiled targets, per-axis doubling chains (raw and on each coarser grid), and splitters that keep base boxes inside the backend's tile limit. Splitters are mandatory, not optional: a target whose window exceeds the limit never gets a Base_ derivation.\n\nThen a topological order, and a liveness pass that assigns physical buffers so peak memory is the maximum live set rather than the sum of all nodes.\n\nBecause kernel_limits differs per backend, so does the plan. One plan run on two backends is bitwise identical; the same request on two backends agrees to rounding.","title":"Choosing"},{"category":"section","location":"concepts/planner/#Reading-a-plan","page":"The planner","text":"p = prepare(x, [2, 4, 8, 16, 32, 64]; stats = (Mean(), Var()))\nexplain(p)\n\nreports the derivation of every node, the number of passes over the input, bytes read and written, merges, and peak workspace bytes.","title":"Reading a plan"},{"category":"section","location":"concepts/planner/#What-it-will-not-do","page":"The planner","text":"Summed-area tables and other prefix-difference tricks are rejected on purpose. They need unmerge, and subtracting accumulators is exactly the cancellation-prone form the numerics policy rules out for means, variances and covariances. unmerge stays in the algebra for callers who want it; it never appears in a plan.","title":"What it will not do"},{"category":"section","location":"concepts/backends/#Backends","page":"Backends","text":"Every entry point takes a backend keyword, dispatched on the abstract types of ComputationalBackends.jl:\n\nusing ComputationalBackends: ComputationalBackends as CB\n\nblockstats(x, [8]; stats = (Mean(),), backend = CB.SerialBackend())\nblockstats(x, [8]; stats = (Mean(),), backend = CB.ThreadedBackend())\nblockstats(x, [8]; stats = (Mean(),), backend = CB.GPUBackend(KernelAbstractions.CPU()))\n\nCB.AutoBackend() is the default: a GPU backend when the fields are device arrays and the KernelAbstractions extension is loaded, else threads when OhMyThreads is loaded and more than one is available, else serial.\n\nA backend whose extension is not loaded says so rather than failing obscurely.","title":"Backends"},{"category":"section","location":"concepts/backends/#Serial","page":"Backends","text":"The reference implementation. Box loops run innermost along the contiguous axis; axes whose windows have uniform length ≤ 8 get compile-time lengths, so boxes of at most 32 cells unroll into one balanced expression tree and longer rows unroll per row.","title":"Serial"},{"category":"section","location":"concepts/backends/#Threaded-—-using-OhMyThreads","page":"Backends","text":"tforeach over contiguous chunks of output cells, four chunks per thread. No cell is written by two tasks and the merge order inside a cell is unchanged, so a threaded run of a given plan is bitwise identical to a serial run of the same plan.\n\nThreading does not make bandwidth-bound work arbitrarily faster. On a 96-core machine a threaded streaming sum is only 2.75× a serial one at 8 threads, so that — not the thread count — is the ceiling.","title":"Threaded — using OhMyThreads"},{"category":"section","location":"concepts/backends/#GPU-—-using-KernelAbstractions-(plus-CUDA-for-launch-tuning)","page":"Backends","text":"One workitem per output cell, reducing its box in registers with the same combine the CPU runs. Nothing is shared between workitems, so there is no local memory and no barrier.\n\nAccumulator storage is struct-of-arrays in the input's own array type, so a plan allocated from a device array is already device-resident: results stay on the device and nothing transfers except finalized scalars. The workspace copies any index data the kernels dereference into the storage's array type once, at allocate time.\n\nA device reports scan_ok = false — the two-stack sliding scan carries a dependency along its axis — so the planner composes instead. A per-cell scan kernel exists for a caller who builds that step by hand.\n\nParity is tested on KernelAbstractions.CPU() and JLArrays on every run; real CUDA hardware is a separate gated job in gpu/.","title":"GPU — using KernelAbstractions (plus CUDA for launch tuning)"},{"category":"section","location":"concepts/backends/#Across-processes","page":"Backends","text":"MPI and Distributed have their own page: partitioned tensors.","title":"Across processes"},{"category":"section","location":"concepts/distributed/#Partitioned-tensors","page":"Partitioned tensors","text":"Two backends compute over a tensor no single process holds. They share everything below the transport — the same slab geometry, the same two plans, the same kernels — and differ only in how the halo arrives.","title":"Partitioned tensors"},{"category":"section","location":"concepts/distributed/#The-shape-of-the-problem","page":"Partitioned tensors","text":"Split a tensor along one axis. A window belongs to the slab its first cell falls in, so every window reaches forward only and one halo, on one side, is enough.\n\nEach rank's windows then divide in two:\n\nthose its own slab covers, computed straight off its array;\nthose that reach past its end, computed over a small staging region holding just the cells they read.\n\nBoth write disjoint slices of one set of result arrays, so nothing is copied to stitch them together.\n\nWhat travels is raw input cells, not accumulators. An accumulator is wider than an input element and the missing cells are the same either way, so for dense and strided windows the halo is strictly cheaper; for large pure tiles accumulators would be cheaper, but they cannot express the leading, clipped pieces that overlapping windows produce. One halo, exchanged once before any compute, handles every window family.","title":"The shape of the problem"},{"category":"section","location":"concepts/distributed/#MPI-—-using-MPI","page":"Partitioned tensors","text":"SPMD: each rank already holds its slab and passes it as Partitioned.\n\nusing MPI, ComputationalBackends\nMPI.Init()\n\nr = blockstats(Partitioned(my_slab; axis = 3), [4, 8, 16];\n               stats = (Mean(), Var()),\n               backend = CB.MPIBackend(CB.SerialBackend(), MPI.COMM_WORLD))\n\nPartitioned(fields; axis) says only which axis is split; the global tensor is the concatenation of every rank's fields along it, in rank order, and an Allgather of the local extents supplies the rest. Passing comm = nothing to MPIBackend means MPI.COMM_WORLD.\n\nResults stay partitioned: each rank gets the ordinary ScaleResults for the windows it owns, with the windows in global coordinates so geometry reports where they sit in the whole tensor.","title":"MPI — using MPI"},{"category":"section","location":"concepts/distributed/#Distributed-—-using-Distributed","page":"Partitioned tensors","text":"One process owns the tensor and scatters slabs of the last axis to the workers.\n\nusing Distributed\naddprocs(4)\n@everywhere using BlockwiseStatisticalReductions\n\nr = blockstats(x, [4, 8, 16]; stats = (Mean(), Var()),\n               backend = CB.DistributedBackend(CB.SerialBackend()))\nrelease!(p)   # give the worker buffers back when done with a prepared handle\n\nEvery worker needs @everywhere using BlockwiseStatisticalReductions; the request says so plainly if one does not have it.\n\nA worker keeps its buffers and its prepared plan between calls, so what crosses a process boundary per call is the slab's cells out and its share of the results back — never a plan, and never the whole tensor at once. That transfer is inherent to a single owner; if the data is already on the workers, MPI is the better fit.","title":"Distributed — using Distributed"},{"category":"section","location":"concepts/distributed/#Weights","page":"Partitioned tensors","text":"Per-element weights are partitioned like the data and travel in the same halo. Per-axis weight factors index the global axis, so every rank slices its own share out of the same vector — a factor along an axis is a property of the axis, not of who happens to hold which cells.","title":"Weights"},{"category":"section","location":"concepts/distributed/#Accuracy","page":"Partitioned tensors","text":"A rank derives its windows from a different plan than a single process does, so cross-process results agree to rounding rather than bit for bit — the same rule as across backends.","title":"Accuracy"},{"category":"section","location":"concepts/labeled/#Labelled-axes","page":"Labelled axes","text":"","title":"Labelled axes"},{"category":"section","location":"concepts/labeled/#Names-and-coordinates,-without-a-dependency","page":"Labelled axes","text":"Two keywords carry everything the core needs:\n\nblockstats(x, spec; stats = (Mean(),),\n           dimnames = (:x, :y, :z),\n           spacing = (Regular(0.5), Regular(0.5), Edges(z_edges)))\n\ndimnames lets a scale specification address axes by name.\nspacing gives each axis its coordinates, so sizes can be given in physical units and results can report where each output cell sits.\n\nAn axis spacing is Regular(step; first) for evenly spaced cells or Edges(v) for extent + 1 explicit edges. spacing_from_points(centers) builds one from cell centres: evenly spaced centres give a Regular, otherwise the edges are the midpoints between neighbours with the outermost two reflected.","title":"Names and coordinates, without a dependency"},{"category":"section","location":"concepts/labeled/#Sizes-in-physical-units","page":"Labelled axes","text":"ScaleSet((Dyadic(; max = Length(2000.0)), Dyadic(; max = Length(2000.0)), Fixed(1)))\n\nLength(x) is resolved against the axis' spacing: a minimum becomes the smallest window whose mean physical span is at least x, a maximum the largest whose span is at most x.","title":"Sizes in physical units"},{"category":"section","location":"concepts/labeled/#Where-an-output-cell-is","page":"Labelled axes","text":"g = geometry(r, (8, 8))\ng.bounds[1]     # (low, high) coordinate of every output cell along axis 1\ng.centers[1]\n\nUnder a non-uniform Edges spacing the physical span of an output cell is the sum of the native spans it covers, which is what you want and what a naive size × Δ would get wrong.","title":"Where an output cell is"},{"category":"section","location":"concepts/labeled/#DimensionalData","page":"Labelled axes","text":"With DimensionalData loaded, blockstats on a DimArray or DimStack takes the names and coordinates from the array and gives them back:\n\nusing DimensionalData\nr = blockstats(A, [8]; stats = (Mean(), Var()))\nr[(8, 8)]        # a DimStack whose lookups carry the true bounds of each output cell\n\nOutput axes are rebuilt as Sampled lookups with an Explicit span and Intervals(Center()) sampling, so each output cell carries the real extent of the window it came from — not a point.\n\nThe result is still a ScaleResults, so scales, windows, geometry and explain keep working.","title":"DimensionalData"},{"category":"section","location":"concepts/labeled/#NetCDF-and-Zarr","page":"Labelled axes","text":"Both carry labelled axes of their own, so both can be read without DimensionalData.\n\nusing NCDatasets\nds = NCDataset(\"f.nc\")\n\nblockstats(ds[\"temperature\"], [8]; stats = (Mean(),))            # one variable\nblockstats(ds, (:u, :w), [8]; stats = (Cov(:u, :w),))            # several, as named fields\n\nusing Zarr\ng = zopen(\"store.zarr\")\n\nblockstats(g[\"temperature\"], [8]; stats = (Mean(),), group = g)  # `group` supplies the coordinates\nblockstats(g, (:u, :w), [8]; stats = (Cov(:u, :w),))\n\nAxis names come from the variable's own dimensions — the file's dimension names for NetCDF, the _ARRAY_DIMENSIONS attribute for Zarr — and coordinates from a coordinate variable of the same name where one exists. A Zarr array opened on its own has no group to look in, so pass group for coordinates; an array with no dimension names at all needs an explicit dimnames.\n\nThe variable is materialized before reducing: these are chunked, possibly remote stores, and the kernels want an array. prepare once and blockstats! per variable to reuse the plan across a directory of files.","title":"NetCDF and Zarr"},{"category":"section","location":"#BlockwiseStatisticalReductions.jl","page":"Home","text":"Mergeable statistics of an N-dimensional tensor over many window sizes at once, computed by a planned tree of reductions that touches the data as close to once as possible — on CPU, GPU, and across processes.\n\nusing BlockwiseStatisticalReductions\n\nx = randn(4096, 4096)\nr = blockstats(x, [2, 4, 8, 16, 32, 64]; stats = (Mean(), Var()))\n\nr[(8, 8)].mean        # 512×512 array of tile means\nr[(64, 64)].var       # 64×64 array of tile variances\nscales(r)             # the sizes that were produced\n\n(Image: Tile means at four scales from one pass)\n\nThose six scales cost one pass over the input. Each level is built by merging the level below it, not by re-reading x, so the whole tower runs at about twice the time of a single sum(x) — where computing the six independently would cost about seven.\n\n(Image: Cost against the number of scales)\n\n(Image: The plan for five tile sizes, against one pass each)","title":"BlockwiseStatisticalReductions.jl"},{"category":"section","location":"#What-it-is-for","page":"Home","text":"You have a big array and you want the same statistic at many resolutions: a variance at 2×2, 4×4, …, 64×64 tiles; a covariance in sliding 16×16 windows every 4 cells; a mean over anisotropic 3-D boxes whose vertical extent is fixed in metres rather than cells. Doing that naively means one pass per size. This package works out how to share the work instead.\n\nThe idea is that every statistic here is a mergeable monoid over sufficient statistics — a variance is (n, mean, M2), and two of those combine into one without revisiting the data. Given that, a window of size 64 is the merge of two windows of size 32, a window of size 12 is the merge of a window of size 8 and a window of size 4 shifted by 8, and so on. The planner searches that space of derivations and picks the cheapest tree.","title":"What it is for"},{"category":"section","location":"#Where-to-start","page":"Home","text":"Statistics — what an accumulator is, which ones ship, and how several are fused into one pass.\nWindows — tiles, strides, anchors, and what happens at the edges.\nScale specifications — how to ask for a set of window sizes.\nThe planner — how the tree is chosen, and why it is fast.\nNumerics — Welford, Chan/Pébay, and shifted accumulation.\nWeights and gaps — weighted statistics and skipping non-finite data.\nLabelled axes — axis names, coordinates, DimensionalData, NetCDF, Zarr.\nBackends — serial, threaded, GPU.\nPartitioned tensors — MPI and Distributed.\nPerformance — what it costs, and how to check that on your machine.\nAPI reference.","title":"Where to start"},{"category":"section","location":"#Installation","page":"Home","text":"import Pkg\nPkg.add(url = \"https://github.com/jbphyswx/BlockwiseStatisticalReductions\", subdir = \"julia/BlockwiseStatisticalReductions.jl\")","title":"Installation"}]
}
